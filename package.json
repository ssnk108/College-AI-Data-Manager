{
  "name": "college-ai-data-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Complete MERN stack app for managing college data with Groq AI assisted extraction.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

